# Supabase Configuration
# Get these values from your Supabase project settings
# Note: Variables must be prefixed with VITE_ to be available in renderer process
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# GitHub Token for Private Repository Auto-Updates
# Required for auto-updates to work with private repositories
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required permissions: Only "repo" -> "public_repo" OR for private repos just read access
# Fine-grained token: Select "Contents" with "Read-only" access
GH_TOKEN=your-github-token-here
